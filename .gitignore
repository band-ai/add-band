# Python / pytest artifacts
__pycache__/
*.pyc
.pytest_cache/

# Claude Code local state & worktrees
.claude/

# Local-only bootstrap variants (e.g. editable installs for live testing) — never shipped
*/bootstrap.local.sh
